---
- hosts: localhost
  connection: local 
  become: true

  pre_tasks:
    - name: Update Repo's
      apt:
        update_cache: yes
        upgrade: yes
        autoremove: yes
        autoclean: yes
      changed_when: False

  roles:
    - base
