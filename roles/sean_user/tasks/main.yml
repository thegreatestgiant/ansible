- name: Create/Ensure my user
  user:
    name: "{{ user.name }}"
    password: "{{ user.password | password_hash('sha512') }}"
    groups: sudo, docker
    shell: /bin/bash
    append: yes

- name: Set-up ssh for my user
  include_role:
    name: ssh


