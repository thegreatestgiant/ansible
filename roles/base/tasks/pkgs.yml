- name: Install All Packages
  apt:
    name: 
      - sudo
      - nano
      - net-tools
      - nautilus
      - bat
      - sshfs
      - zip
      - unzip
      - tree
      - git
      - gnupg
      - fuse3
      - wget
      - cron
      - python3-passlib
      - ripgrep
    state: present
  changed_when: False

- name: Install Gh
  include_tasks: gh_stuff.yml
